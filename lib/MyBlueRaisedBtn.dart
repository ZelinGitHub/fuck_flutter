import 'package:flutter/material.dart';

class MyBlueRaisedBtn extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return RaisedButton(
      onPressed: () {},
      color: Colors.blue,
      child: Text("天空"),
    );
  }
}
