import 'package:flutter/material.dart';


class EmailScaffold extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("email"),
      ),
      body: Center(
        child: Text("email"),
      ),
    );
  }
}
