import 'package:flutter/material.dart';

class MyRedRaisedBtn extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return RaisedButton(
      onPressed: () {},
      color: Colors.red,
      child: Text("血"),
    );
  }
}
